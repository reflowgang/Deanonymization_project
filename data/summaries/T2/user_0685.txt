Still not good enough to compile most of their "open source" projects. Windows had it first and then it was added to Linux (at least that's what I'm told). The reason this isn't a mistake is that most desktop apps (unlike mobile apps) are open source and community maintained. Command line apps work pretty well on android, I use them every day. They're in the process of rewriting the DOM handler to support dynamic changes from JavaScript. JavaScript itself runs well. Maybe it would be better to just specify the length of the source file in bytes, so we don't have these weird arguments about line length. Have you ever tried hacking on any of Google's "open source" projects? Android is ~100GB just to download for example, but I think chromium is smaller. Critical systems should be open source by law. Any android ROM is still primarily built for consumption at the end of the day. The good news is that FB has behaved so poorly that almost no one I know uses it or trusts it any more, so we know that in the long run at least this kind of behavior doesn't work.